fastapi==0.115.0
uvicorn[standard]==0.30.6
# Required by FastAPI for `Form(...)` params — the Twilio webhooks in
# app/api/v1/telephony.py fail to import without it, which silently drops the
# entire telephony router (including /telephony/voice/outbound).
python-multipart==0.0.20
pydantic==2.9.2
pydantic-settings==2.5.2
sqlalchemy==2.0.35
asyncpg==0.29.0
psycopg2-binary==2.9.9
alembic==1.13.3
redis==5.0.8
twilio==9.3.2
httpx==0.27.2
python-dotenv==1.0.1
tenacity==9.0.0
email-validator>=2.2.0
websockets>=12.0
python-jose[cryptography]>=3.3.0
passlib==1.7.4
bcrypt==4.0.1
pyjwt>=2.8.0
python-dateutil>=2.9.0
# Ships the IANA tz database for zoneinfo. Required for per-spa timezones in
# app/services/business_hours.py: Windows has no system tz database at all, and
# slim Linux images frequently omit it.
tzdata>=2024.1

# --- Tests ---
pytest==8.3.3
pytest-asyncio==0.24.0
